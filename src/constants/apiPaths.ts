
const path = 'https://791sp7dv1a.execute-api.eu-west-1.amazonaws.com/prod'
const API_PATHS = {
  product: `${path}`,
  order:  `${path}`,
  import:  `https://naeo5jd2kl.execute-api.eu-west-1.amazonaws.com/prod`,
  bff: `${path}`,
  cart: `${path}`,
};

export default API_PATHS;
